#include<stdio.h>
#include<stdlib.h>
void main()
{
file*fp;
char ch;
int space=0;line=0;
if(fp==null)
{
printf("\n  file not fount");
exit;
else
}
if(ch=EOF)
{
if (ch=' ');
space++;
if(ch=  \t);
line++
printf("\n number of space");
printf(\n number of line");
return 0
}
